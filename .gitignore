# The vault file is currently PLAINTEXT and holds the live k3s join token.
# It stays ignored until `ansible-vault encrypt` has been run on it; an
# encrypted vault file is safe to commit, and this line should be dropped then.

*.retry
.vault-pass
kubeconfig
*.tgz
sealed-keys-*.yaml
__pycache__/
.ansible_facts/
backups/
